function addNote(){
    var wrapper = document.querySelector(".wrapper");
    wrapper.classList.add("overlay");
    var modal = document.querySelector(".modal");
    modal.classList.add("show");

   return function validateModal() {
        alert("hi");

    }
}
